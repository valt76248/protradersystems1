
# Анализ архитектуры приложения и рефакторинг (LLM-AI Coding Principles)

На основе анализа кодовой базы и принципов стабильного фронтенд-программирования, предлагается следующий план улучшений. Основная цель — устранение "черных экранов", повышение надежности и очистка от технического долга.

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ

### 1. Устранение "черных экранов" (Crash Prevention)

* **Решение:** (РЕАЛИЗОВАНО) Добавлен `ErrorBoundary` на верхнем уровне и вокруг `Suspense`. Теперь любой крах компонента покажет понятное сообщение об ошибке и кнопку "Обновить", а не пустой экран.
* **Маршрутизация:** Пересмотрено расположение `MaintenanceGuard` и `BrowserRouter` для обеспечения стабильности состояния роутера.

### 2. Исправление критической ошибки в Admin.tsx

* **Проблема:** Вызов `supabase.auth.admin.getUserById` приводил к немедленному падению приложения на стороне клиента, так как `admin` API доступен только с сервисным ключом.
* **Решение:** (РЕАЛИЗОВАНО) Переход на использование публичной таблицы `profiles` для получения данных пользователей.

### 3. Обработка ошибок загрузки чанков

* **Проблема:** При нестабильном интернете `lazy import` может не загрузить файл, что ведет к "черному экрану".
* **Решение:** `ErrorBoundary` теперь перехватывает ошибки загрузки модулей.

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### 1. Полная локализация (Technical Debt)

* **Проблема:** Файлы `BeginnerTraining.tsx`, `Session1Gallery.tsx`, `Admin.tsx` и `MaterialsManager.tsx` содержат много жестко закодированного текста (hardcoded strings).
* **Действие:** Перенести все строки в `ru.ts` и `uk.ts`. Это критично для того, чтобы LLM-ассистент мог эффективно работать с текстом в будущем.

### 2. Очистка "артефактов" копипаста

* **Проблема:** В формах (например, `PreRegistrationForm.tsx`) встречались упоминания "целителя" — это остатки от других проектов, которые путают пользователя и ИИ.
* **Решение:** (РЕАЛИЗОВАНО) Замена на "ProTrader Systems".

### 3. Консолидация хуков Toast

* **Проблема:** `useToast` импортируется из `@/hooks/use-toast` и `@/components/ui/use-toast`.
* **Действие:** Оставить только `@/components/ui/use-toast` для единообразия.

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ

### 1. Удаление неиспользуемого кода

* **Файлы:** Проверить и удалить неиспользуемые изображения в `public/images/`.
* **Зависимости:** Удалить `stripe` и `@stripe/stripe-js`, если оплата идет только через крипто-переводы, чтобы облегчить бандл.

### 2. Улучшение именования маршрутов

* **Предложение:** Переименовать компоненты `SessionETF` в `SessionOne` и `SessionAdvanced` в `SessionTwo` в файловой структуре для соответствия логике курса.

---

## Что было сделано в ходе этого сеанса

1. **Добавлен ErrorBoundary**: Теперь приложение защищено от внезапных падений.
2. **Исправлен `App.tsx`**: Порядок провайдеров оптимизирован, ErrorBoundary внедрен правильно.
3. **Исправлен `Admin.tsx`**: Устранена причина падения страницы админа.
4. **Локализовано SEO для калькуляторов**: Добавлены переводы для мета-тегов.
5. **Чистка текста**: Удалены некорректные упоминания в формах.

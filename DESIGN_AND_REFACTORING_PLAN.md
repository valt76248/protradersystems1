# План улучшения дизайна и UX (LLM-AI Coding Patterns)

На основе обновленных принципов разработки и анализа текущего состояния проекта ProTrader Systems, предлагается следующий план по улучшению визуальной составляющей и пользовательского опыта. Основной упор сделан на "премиальность", интерактивность и чистоту кода для поддержки ИИ-ассистентов.

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ (Premium Feel & Core UX)

### 1. Плавные переходы между страницами (Page Transitions)

* **Проблема:** При переключении маршрутов контент сменяется мгновенно, что создает ощущение "прыжка".
* **Решение:** Использовать `AnimatePresence` из `framer-motion` в `App.tsx` для реализации плавного исчезновения/появления (Fade-in/out) страниц.
* **Масштабируемость:** Это упрощает восприятие структуры приложения как для пользователя, так и для ИИ-ассистентов, отслеживающих состояние.

### 2. Скелетон-загрузчики (Skeleton Loaders)

* **Проблема:** При загрузке данных из Supabase (например, в профиле или админ-панели) пользователь видит пустые блоки или резкие скачки контента.
* **Решение:** Внедрить компоненты `Skeleton` из `shadcn/ui` для всех динамических данных.
* **Дизайн:** Использовать анимированные градиенты (shimmer effect) в цветах бренда (cyan/indigo).

### 3. Магнитные кнопки (Magnetic Call-to-Action)

* **Идея:** Улучшить основные кнопки (`StartTrainingButton`, `AuraButton`), добавив эффект притяжения к курсору в определенном радиусе.
* **Реализация:** Хук `useMagnetic` на базе `framer-motion`. Это создает эффект "живого" и дорогого интерфейса.

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ (Interactive Polish)

### 1. Кастомный курсор "Crosshair"

* **Концепция:** В разделах, связанных с графиками и аналитикой, стандартный курсор заменяется на элегантное перекрестие (Crosshair), подчеркивающее трейдинговую тематику.
* **Интерактивность:** При наведении на кликабельные элементы перекрестие может превращаться в точку или расширяться.

### 2. Анимированный счетчик статистики

* **Место:** Страница "About" и блоки результатов.
* **Эффект:** Числа (проценты прибыли, количество учеников) должны анимированно "набегать" при скролле до этого блока.

### 3. Интерактивный фон Aura

* **Улучшение:** Сделать `AuraBackground` слегка отзывчивым к движению мыши. Пятна света должны плавно следовать за курсором с большой инерцией, создавая глубину.

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ (Visual Consistency)

### 1. Scroll Progress Bar

* **Описание:** Тонкая неоновая полоса в самом верху экрана, показывающая прогресс прочтения страницы (особенно важно для длинных лендингов).
* **Стиль:** Градиент от `cyan-400` до `blue-600`.

### 2. Текстовые Reveal-анимации

* **Детали:** Заголовки H1 и H2 в Hero-секции должны появляться не целиком, а через посимвольное или построчное проявление (word-by-word reveal).

---

## 🧹 Рефакторинг и Чистка (LLM-Alignment)

### 1. Окончательная локализация (Total Localization)

* **Статус:** Требуется аудит `Session1Gallery.tsx` и `Session2Gallery.tsx`. Там много "зашитого" русского текста в описаниях и заголовках.
* **Приоритет:** СРЕДНИЙ. Для LLM важно, чтобы все текстовые ресурсы были в одном месте (`ru.ts`, `uk.ts`).

### 2. Удаление неиспользуемых ассетов

* **Действие:** Проверить папку `public/` на наличие старых скриншотов и иконок, которые больше не используются в текущей версии дизайна.

### 3. Переименование маршрутов

* **Предложение:** Переименовать `SessionETF.tsx` -> `SessionOne.tsx` и `SessionAdvanced.tsx` -> `SessionTwo.tsx`. Это сделает код самодокументируемым и соответствующим логике курса (Урок 1, Урок 2).

---

## ✅ Резюме изменений

* **Эстетика:** Больше динамики, глубины и микро-взаимодействий.
* **Надежность:** Контроль состояний загрузки (Skeletons).
* **Чистота:** Единство локализации и логичное именование файлов.

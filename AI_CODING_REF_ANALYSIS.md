# Анализ архитектуры и план рефакторинга (LLM-AI Coding Edition)

Этот документ содержит комплексный анализ приложения ProTrader Systems с точки зрения принципов **LLM-ориентированного программирования**. Основная цель — сделать код максимально понятным для ИИ-агентов и эффективным для поддержки человеком.

---

## 🧠 Принципы ИИ-ориентированного кодинга

1. **Self-Explaining Structure**: Названия функций и файлов должны говорить сами за себя.
2. **Flat & Modular**: Избегаем глубокой вложенности. Предпочитаем модули с четкими границами.
3. **Separation of Concerns**: UI — только отображает, Service — только логика, Data — только константы.
4. **No Dead Code**: Неиспользуемые файлы и закомментированный код — это "шум" для контекста ИИ.
5. **Single Source of Truth**: Локализация через контекст, стили через токены, конфиги через `.env`.

---

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ

### 1. Удаление "зомби-кода" (Мертвые файлы)

В папке `src/pages` обнаружены файлы-заглушки, которые не используются в `App.tsx` или содержат пустой UI. Это путает ИИ при поиске путей навигации.

* **Действие**: Удалить `Indicators.tsx`, `Strategies.tsx`.
* **Обоснование**: Эти страницы заменены разделом "Visual Guide" и новой структурой курсов.

### 2. Вынос логики из UI-компонентов

В `Header.tsx` функция `fetchUserProfile` работает напрямую с Supabase. Это нарушает принцип изоляции сервисов.

* **Действие**: Переместить `fetchUserProfile` в `src/services/authService.ts`.
* **Обоснование**: Централизация работы с пользователем позволяет проще менять провайдера аутентификации в будущем.

### 3. Завершение рефакторинга N8N Lab (✅ СДЕЛАНО)

Ранее система имела жестко зашитые ворклоу внутри гигантского файла.

* **Что сделано**:
  * Данные вынесены в `src/data/n8nWorkflows.ts`.
  * Логика API вынесена в `src/services/n8nService.ts`.
  * UI полностью обновлен с использованием `Tabs` и `PremiumCard`.

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### 1. Переименование для ясности

* `src/components/layout/Header.tsx` -> `src/components/layout/Navbar.tsx`. (Header звучит как заголовок секции, Navbar чётче определяет роль).
* `src/components/shared/StartTrainingButton.tsx` -> `src/components/ui/ActionStartButton.tsx`.

### 2. Типизация API ответов

Многие вызовы в сервисах используют `any`. Для ИИ важно видеть контракты данных, чтобы не генерировать ошибки доступа к полям.

* **Действие**: Добавить интерфейсы для всех возвращаемых данных из Supabase в `src/types/database.types.ts`.

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ

### 1. Локализация N8N Lab

Сейчас описание ворклоу в новой лаборатории на английском.

* **Действие**: Добавить ключи `lab.*` в `ru.ts` и `uk.ts`.

### 2. Оптимизация Vite Proxy

Сгруппировать все внешние API (Brevo, n8n, Supabase) в единый конфиг.

---

## 📊 Таблица изменений

| Файл/Директория | Статус | Изменение | Причина |
| :--- | :--- | :--- | :--- |
| `src/pages/N8NLab.tsx` | ✅ Refactored | Полное разделение UI и данных | Повышение читаемости и Premium дизайн |
| `src/data/n8nWorkflows.ts`| ✨ New | Вынос констант | Уменьшение размера компонента |
| `src/services/n8nService.ts`| ✨ New | Изоляция API | Упрощение тестирования |
| `src/pages/Indicators.tsx` | 🗑️ Delete | Удаление файла | Не используется (Zombie code) |
| `src/pages/Strategies.tsx` | 🗑️ Delete | Удаление файла | Не используется (Zombie code) |

---

## ✅ Описание проделанной работы

1. **N8N Lab**: Теперь это полноценный диагностический центр с вкладками "Simulator" (для тестов) и "Documentation" (описание логики). Добавлены премиальные эффекты `PremiumCard` и `AuraBackground`.
2. **Структура данных**: Все ворклоу (Pre-registration, Referral, NPS и т.д.) теперь имеют детальные описания, что помогает ИИ понимать роль каждой автоматизации в экосистеме.
3. **Безопасность**: Запросы проксируются через `/n8n-api`, скрывая реальный адрес сервера в браузере при локальной разработке.

**Следующий шаг**: Выполнить физическое удаление файлов и коррекцию `Header.tsx`.


# Отчет по рефакторингу и анализу приложения ProTrader Systems

## 🏁 Что было сделано (Высокий приоритет)

### 1. Консолидация компонентов Аутентификации

- **Проблема**: Разрозненные формы логина и регистрации с дублирующейся логикой и хардкодом.
- **Решение**: Созданы `LoginForm.tsx` и `RegisterForm.tsx` как функционально полные компоненты. Внедрен `AuthFlow.tsx` для управления переключением между ними.
- **Результат**: Единая точка входа для авторизации, поддержка локализации, исправленные ошибки доступности (aria-labels).

### 2. Очистка от "Зомби-кода" и Файлов

- Убрана папка `speak-trade-lectures` (содержала устаревшие лекции).
- Удалены пустые страницы-заглушки: `Indicators.tsx` и `Strategies.tsx`.
- Удален `courseService.ts`, который использовал `localStorage` вместо БД.

### 3. Унификация Хранения Данных

- Компонент `CourseOverview` переведен с `localStorage` на **Supabase**. Теперь данные на главной странице соответствуют тому, что настраивается в панели управления `CourseStructure`.

### 4. Переименование для ясности (Naming Standards)

- `Index.tsx` → `Home.tsx` (Главная страница).
- `course` → `course-admin` (Компоненты управления).
- `courses` → `course-ui` (Компоненты отображения).
- `SessionOne/Two` → `SessionETF/SessionAdvanced` (Более описательные названия).

---

## 🔝 Рекомендации по дальнейшему улучшению

### Высокий приоритет (Технический долг)

1. **Полная локализация данных сессий**: Сейчас тексты в `src/data/session1.ts` и `session2.ts` захардкожены на русском. Их нужно вынести в `locales` или в БД Supabase с поддержкой языков.
2. **SEO Оптимизация**: Большинство страниц используют дефолтный или минимальный SEO-контент. Нужно прописать уникальные мета-теги для каждой страницы через компонент `SEO`.

### Средний приоритет (Архитектура)

1. **Типизация Supabase**: Рекомендуется сгенерировать типы Supabase (через CLI) и использовать их вместо ручного приведения типов `as unknown as CourseModule`.
2. **Обработка ошибок (Error Boundaries)**: Добавить глобальный и компонентные перехватчики ошибок, чтобы приложение не "падало" при сбоях в API.

### Низкий приоритет (Полировка)

1. **Skeleton Loaders**: Заменить стандартные спиннеры на анимированные скелетоны для улучшения восприятия скорости загрузки.
2. **Интерактивные калькуляторы**: Доделать локализацию для оставшихся 4 калькуляторов (Margin, Swap и др.).

---

## 📂 Структура проекта после изменений

- `src/components/auth/`: Новые унифицированные компоненты входа.
- `src/components/course-admin/`: Формы и управление структурой.
- `src/components/course-ui/`: Карточки и секции для пользователей.
- `src/pages/Home.tsx`: Чистая и переименованная главная страница.
- `src/data/locales/`: Централизованные словари (uk.ts / ru.ts).
